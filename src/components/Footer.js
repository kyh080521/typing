import style from "../styles/Footer.module.css";

const Footer = () => {
    return (
        <div className={style.Footer}>
            <span className={style.footDescription}>Start typing to begin.</span>
        </div>
    )
}

export default Footer;